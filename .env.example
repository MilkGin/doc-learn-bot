# Ollama 本地服务地址（默认本机）
OLLAMA_BASE_URL=http://localhost:11434

# 用于生成回答的大模型（需本地已拉取，机器配置低可换 qwen2.5:3b）
LLM_MODEL=qwen2.5:7b

# 用于文档向量化的嵌入模型（需本地已拉取）
EMBED_MODEL=nomic-embed-text

# 技术文档存放目录（支持 .md / .txt / .pdf，可含子目录）
DOCS_DIR=./docs

# 向量库持久化目录
CHROMA_DIR=./chroma_db

# 每次检索召回的片段数量
TOP_K=4
